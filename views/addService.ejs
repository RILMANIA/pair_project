<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            margin: 0;
            background-color: #f4f7f6;
            color: #212529;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .main-container {
            width: 100%;
            max-width: 900px;
            margin: 30px auto;
            padding: 0 20px;
            box-sizing: border-box;
        }

        .btn-back {
            display: inline-block;
            text-decoration: none;
            color: #004a94;
            font-weight: 600;
            font-size: 15px;
            margin-bottom: 20px;
            transition: color 0.2s;
        }

        .btn-back:hover {
            color: #003870;
            text-decoration: underline;
        }

        .form-card {
            width: 100%;
            background-color: white;
            border-radius: 8px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
            overflow: hidden;
            border: 1px solid #dfe4ea;
        }

        .form-header {
            padding: 25px 30px;
            border-bottom: 1px solid #f0f1f3;
            background-color: #fbfcfd;
        }

        .form-header h1 {
            margin: 0;
            font-size: 24px;
            color: #2c3e50;
            font-weight: 700;
        }

        .form-header p {
            margin: 5px 0 0 0;
            font-size: 14px;
            color: #666;
        }

        .form-content {
            padding: 30px 40px;
        }

        .alert-error {
            background-color: #fbebed;
            color: #c0392b;
            border: 1px solid #f5c6cb;
            padding: 15px;
            border-radius: 6px;
            margin-bottom: 25px;
            font-size: 14px;
        }

        .form-grid {
            display: grid;
            grid-template-columns: 180px 1fr;
            gap: 25px;
            align-items: center;
        }

        .form-grid label {
            font-weight: 600;
            color: #34495e;
            font-size: 14px;
        }

        .label-top {
            align-self: start;
            margin-top: 12px;
        }

        .form-input {
            width: 100%;
            padding: 12px 15px;
            border: 1px solid #ced4da;
            border-radius: 4px;
            font-family: inherit;
            font-size: 15px;
            box-sizing: border-box;
            color: #333;
            transition: border-color 0.2s ease, box-shadow 0.2s ease;
        }

        .form-input:focus {
            outline: none;
            border-color: #004a94;
            box-shadow: 0 0 0 3px rgba(0, 74, 148, 0.1);
        }

        textarea.form-input {
            resize: vertical;
            min-height: 100px;
        }

        .form-actions {
            grid-column: 2 / 3;
            display: flex;
            justify-content: flex-end;
            margin-top: 10px;
        }

        .btn-modern {
            display: inline-block;
            background-color: #004a94;
            color: white;
            padding: 12px 30px;
            border-radius: 25px;
            font-weight: 600;
            font-size: 16px;
            border: none;
            cursor: pointer;
            font-family: inherit;
            transition: background-color 0.2s;
        }

        .btn-modern:hover {
            background-color: #003870;
        }

        .input-helper {
            font-size: 12px;
            color: #888;
            margin-top: 5px;
            display: block;
        }

        .navbar {
            width: 100%;
            background-color: #004a94;
            color: white;
            padding: 0 30px;
            box-sizing: border-box;
            display: flex;
            justify-content: center;
            height: 70px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            margin-bottom: 0;
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .navbar-content {
            width: 100%;
            max-width: 1100px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .navbar-brand {
            font-size: 22px;
            font-weight: 700;
            color: white;
            text-decoration: none;
            display: flex;
            align-items: center;
            letter-spacing: 0.5px;
        }

        .navbar-links {
            display: flex;
            align-items: center;
            gap: 25px;
        }

        .navbar-links a {
            text-decoration: none;
            font-size: 15px;
            font-weight: 500;
            transition: color 0.2s;
            display: flex;
            align-items: center;
        }

        .navbar-links a:hover {
            color: white;
        }

        .navbar-biasa {
            color: #e0e0e0;
        }

        .user-greeting {
            font-size: 14px;
            color: #bbdefb;
            font-weight: normal;
            margin-right: 10px;
            border-right: 1px solid #ffffff50;
            padding-right: 20px;
        }

        .user-greeting strong {
            color: white;
            font-weight: 600;
        }

        .btn-nav-auth {
            background-color: white;
            color: #004a94;
            padding: 8px 20px;
            border-radius: 25px;
            font-weight: 600;
            font-size: 14px;
            border: none;
            cursor: pointer;
            font-family: inherit;
            text-decoration: none !important;
            transition: background-color 0.2s, color 0.2s, transform 0.2s;
        }

        .btn-nav-auth:hover {
            background-color: #f0f0f0;
            color: #003870;
            transform: translateY(-1px);
        }

        .btn-logout {
            background-color: rgba(255, 255, 255, 0.1);
            color: white;
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        .btn-logout:hover {
            background-color: rgba(255, 255, 255, 0.2);
            color: white;
        }
    </style>
</head>

<body>

    <%- include('partial/_navbar.ejs') %>

        <main class="main-container">

            <a href="/services" class="btn-back">← Kembali ke Daftar Layanan</a>

            <div class="form-card">

                <div class="form-header">
                    <h1>Tambah Layanan Baru</h1>
                    <p>Pastikan data layanan diisi dengan lengkap dan benar.</p>
                </div>

                <div class="form-content">

                    <% if (typeof error !=='undefined' && error) { %>
                        <div class="alert-error">
                            <%= error %>
                        </div>
                        <% } %>

                            <form action="/services/add" method="POST" class="form-grid">

                                <label for="name">Nama Layanan</label>
                                <div>
                                    <input class="form-input" type="text" id="name" name="name"
                                        placeholder="Contoh: Cuci Mobil Premium" required>
                                </div>

                                <label for="category">Kategori</label>
                                <div>
                                    <select class="form-input" id="category" name="CategoryId" required>
                                        <option value="" disabled selected>-- Pilih Kategori --</option>

                                        <% categories.forEach(cat=> { %>
                                            <option value="<%= cat.id %>">
                                                <%= cat.name %>
                                            </option>
                                            <% }) %>

                                    </select>
                                </div>

                                <label for="price">Harga Dasar (Rp)</label>
                                <div>
                                    <input class="form-input" type="number" id="price" name="price" placeholder="15000"
                                        min="1000" required>
                                    <span class="input-helper">* Harga per unit/layanan</span>
                                </div>

                                <label for="imgUrl">URL Gambar</label>
                                <div>
                                    <input class="form-input" type="text" id="imgUrl" name="imgUrl"
                                        placeholder="https://...">
                                    <span class="input-helper">* Masukkan link gambar langsung (JPG/PNG)</span>
                                </div>

                                <label for="description" class="label-top">Deskripsi</label>
                                <div>
                                    <textarea class="form-input" id="description" name="description"
                                        placeholder="Jelaskan detail layanan ini..."></textarea>
                                </div>

                                <!-- Spacer untuk grid (kosong) -->
                                <div></div>

                                <div class="form-actions">
                                    <button type="submit" class="btn-modern">Simpan Layanan</button>
                                </div>

                            </form>
                </div>

            </div>
        </main>
</body>

</html>