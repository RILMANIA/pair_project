<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Zapzapp</title>

    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            margin: 0;
            background-color: #f4f7f6;
            color: #333;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .main-container {
            width: 100%;
            max-width: 1000px;
            padding: 40px 20px;
            box-sizing: border-box;
        }

        .btn-back {
            display: inline-block;
            text-decoration: none;
            color: #004a94;
            font-weight: 600;
            font-size: 15px;
            margin-bottom: 20px;
            transition: color 0.2s;
        }

        .btn-back:hover {
            text-decoration: underline;
            color: #003870;
        }

        .detail-card {
            background-color: white;
            border-radius: 8px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
            overflow: hidden;
            display: flex;
            flex-direction: column;
            border: 1px solid #dfe4ea;
        }

        @media (min-width: 768px) {
            .detail-card {
                flex-direction: row;
            }

            .detail-image-container {
                width: 50%;
                max-height: 500px;
            }

            .detail-content {
                width: 50%;
                padding: 40px;
                display: flex;
                flex-direction: column;
                justify-content: center;
            }
        }

        .detail-image-container {
            width: 100%;
            background-color: #eee;
        }

        .detail-img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            display: block;
        }

        .detail-content {
            padding: 30px;
        }

        .service-category {
            display: inline-block;
            background-color: #e3f2fd;
            color: #004a94;
            font-size: 12px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            padding: 6px 12px;
            border-radius: 20px;
            margin-bottom: 15px;
        }

        .service-name {
            margin: 0 0 15px 0;
            font-size: 36px;
            font-weight: 700;
            color: #2c3e50;
            line-height: 1.2;
        }

        .service-price {
            font-size: 28px;
            color: #27ae60;
            font-weight: 700;
            margin-bottom: 25px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .price-label {
            font-size: 14px;
            color: #777;
            font-weight: normal;
        }

        .service-desc {
            font-size: 16px;
            line-height: 1.8;
            color: #555;
            margin-bottom: 40px;
            border-top: 1px solid #eee;
            padding-top: 20px;
        }

        .btn-order {
            display: block;
            width: 100%;
            background-color: #004a94;
            color: white;
            text-align: center;
            padding: 16px;
            border-radius: 8px;
            border: none;
            font-size: 18px;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.2s, transform 0.2s;
            box-shadow: 0 4px 6px rgba(0, 74, 148, 0.2);
        }

        .btn-order:hover {
            background-color: #003870;
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(0, 74, 148, 0.3);
        }
        .btn-delete {
            display: block;
            width: 100%;
            background-color: #004a94;
            color: white;
            text-align: center;
            padding: 16px;
            border-radius: 8px;
            border: none;
            font-size: 18px;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.2s, transform 0.2s;
            box-shadow: 0 4px 6px rgba(0, 74, 148, 0.2);
        }

        .btn-delete:hover {
            background-color: #003870;
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(0, 74, 148, 0.3);
        }

        .navbar {
            width: 100%;
            background-color: #004a94;
            color: white;
            padding: 0 30px;
            box-sizing: border-box;
            display: flex;
            justify-content: center;
            height: 70px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            margin-bottom: 0;
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .navbar-content {
            width: 100%;
            max-width: 1100px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .navbar-brand {
            font-size: 22px;
            font-weight: 700;
            color: white;
            text-decoration: none;
            display: flex;
            align-items: center;
            letter-spacing: 0.5px;
        }

        .navbar-links {
            display: flex;
            align-items: center;
            gap: 25px;
        }

        .navbar-links a {
            text-decoration: none;
            font-size: 15px;
            font-weight: 500;
            transition: color 0.2s;
            display: flex;
            align-items: center;
        }

        .navbar-links a:hover {
            color: white;
        }

        .navbar-biasa {
            color: #e0e0e0;
        }

        .user-greeting {
            font-size: 14px;
            color: #bbdefb;
            font-weight: normal;
            margin-right: 10px;
            border-right: 1px solid #ffffff50;
            padding-right: 20px;
        }

        .user-greeting strong {
            color: white;
            font-weight: 600;
        }

        .btn-nav-auth {
            background-color: white;
            color: #004a94;
            padding: 8px 20px;
            border-radius: 25px;
            font-weight: 600;
            font-size: 14px;
            border: none;
            cursor: pointer;
            font-family: inherit;
            text-decoration: none !important;
            transition: background-color 0.2s, color 0.2s, transform 0.2s;
        }

        .btn-nav-auth:hover {
            background-color: #f0f0f0;
            color: #003870;
            transform: translateY(-1px);
        }

        .btn-logout {
            background-color: rgba(255, 255, 255, 0.1);
            color: white;
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        .btn-logout:hover {
            background-color: rgba(255, 255, 255, 0.2);
            color: white;
        }
    </style>
</head>

<body>

    <%- include('partial/_navbar.ejs') %>

        <div class="main-container">

            <a href="/services" class="btn-back">← Kembali ke Daftar Layanan</a>

            <div class="detail-card">

                <div class="detail-image-container">
                    <img src="<%= service.imgUrl || 'https://via.placeholder.com/600x600?text=No+Image' %>"
                        alt="<%= service.name %>" class="detail-img"
                        onerror="this.src='https://via.placeholder.com/600x600?text=Image+Error'">
                </div>

                <div class="detail-content">
                    <div>

                        <span class="service-category">
                            <%= service.Category ? service.Category.name : 'Layanan Umum' %>
                        </span>

                        <h1 class="service-name">
                            <%= service.name %>
                        </h1>

                        <div class="service-price">
                            <%= service.formatRupiah %>
                                <span class="price-label">/ layanan</span>
                        </div>

                        <p class="service-desc">
                            <%= service.description || 'Tidak ada deskripsi untuk layanan ini.' %>
                        </p>
                    </div>


                    <% if (user.role === "Customer") { %>
                        <form action="/orders/create" method="POST">
    
                            <input type="hidden" name="ServiceId" value="<%= service.id %>">

                            <button type="submit" class="btn-order"
                                onclick="return confirm('Apakah Anda yakin ingin memesan layanan <%= service.name %>?')">
                                Pesan Sekarang
                            </button>
                        </form>
                    <% } else { %>

                        <form action="/services/<%= service.id %>/delete" method="get">
                            <button type="submit" class="btn-delete"
                                onclick="return confirm('Apakah Anda yakin ingin menghapus layanan <%= service.name %>?')">
                                Hapus Layanan
                            </button>
                        </form>

                    <% } %>
                </div>

            </div>
        </div>
</body>

</html>